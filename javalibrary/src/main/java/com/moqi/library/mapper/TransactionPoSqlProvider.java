package com.moqi.library.mapper;

import com.moqi.library.mapper.po.TransactionPo;
import com.moqi.library.mapper.po.TransactionPoExample.Criteria;
import com.moqi.library.mapper.po.TransactionPoExample.Criterion;
import com.moqi.library.mapper.po.TransactionPoExample;
import java.util.List;
import java.util.Map;
import org.apache.ibatis.jdbc.SQL;

public class TransactionPoSqlProvider {
    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table Transaction
     *
     * @mbg.generated
     */
    public String insertSelective(TransactionPo row) {
        SQL sql = new SQL();
        sql.INSERT_INTO("Transaction");
        
        if (row.getUserId() != null) {
            sql.VALUES("`user_id`", "#{userId,jdbcType=BIGINT}");
        }
        
        if (row.getBookId() != null) {
            sql.VALUES("`book_id`", "#{bookId,jdbcType=BIGINT}");
        }
        
        if (row.getBorrowDate() != null) {
            sql.VALUES("`borrow_date`", "#{borrowDate,jdbcType=DATE}");
        }
        
        if (row.getReturnDate() != null) {
            sql.VALUES("`return_date`", "#{returnDate,jdbcType=DATE}");
        }
        
        if (row.getStatus() != null) {
            sql.VALUES("`status`", "#{status,jdbcType=CHAR}");
        }
        
        if (row.getCreatedAt() != null) {
            sql.VALUES("`created_at`", "#{createdAt,jdbcType=TIMESTAMP}");
        }
        
        if (row.getUpdatedAt() != null) {
            sql.VALUES("`updated_at`", "#{updatedAt,jdbcType=TIMESTAMP}");
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table Transaction
     *
     * @mbg.generated
     */
    public String selectByExample(TransactionPoExample example) {
        SQL sql = new SQL();
        if (example != null && example.isDistinct()) {
            sql.SELECT_DISTINCT("`id`");
        } else {
            sql.SELECT("`id`");
        }
        sql.SELECT("`user_id`");
        sql.SELECT("`book_id`");
        sql.SELECT("`borrow_date`");
        sql.SELECT("`return_date`");
        sql.SELECT("`status`");
        sql.SELECT("`created_at`");
        sql.SELECT("`updated_at`");
        sql.FROM("Transaction");
        applyWhere(sql, example, false);
        
        if (example != null && example.getOrderByClause() != null) {
            sql.ORDER_BY(example.getOrderByClause());
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table Transaction
     *
     * @mbg.generated
     */
    public String updateByExampleSelective(Map<String, Object> parameter) {
        TransactionPo row = (TransactionPo) parameter.get("row");
        TransactionPoExample example = (TransactionPoExample) parameter.get("example");
        
        SQL sql = new SQL();
        sql.UPDATE("Transaction");
        
        if (row.getId() != null) {
            sql.SET("`id` = #{row.id,jdbcType=BIGINT}");
        }
        
        if (row.getUserId() != null) {
            sql.SET("`user_id` = #{row.userId,jdbcType=BIGINT}");
        }
        
        if (row.getBookId() != null) {
            sql.SET("`book_id` = #{row.bookId,jdbcType=BIGINT}");
        }
        
        if (row.getBorrowDate() != null) {
            sql.SET("`borrow_date` = #{row.borrowDate,jdbcType=DATE}");
        }
        
        if (row.getReturnDate() != null) {
            sql.SET("`return_date` = #{row.returnDate,jdbcType=DATE}");
        }
        
        if (row.getStatus() != null) {
            sql.SET("`status` = #{row.status,jdbcType=CHAR}");
        }
        
        if (row.getCreatedAt() != null) {
            sql.SET("`created_at` = #{row.createdAt,jdbcType=TIMESTAMP}");
        }
        
        if (row.getUpdatedAt() != null) {
            sql.SET("`updated_at` = #{row.updatedAt,jdbcType=TIMESTAMP}");
        }
        
        applyWhere(sql, example, true);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table Transaction
     *
     * @mbg.generated
     */
    public String updateByExample(Map<String, Object> parameter) {
        SQL sql = new SQL();
        sql.UPDATE("Transaction");
        
        sql.SET("`id` = #{row.id,jdbcType=BIGINT}");
        sql.SET("`user_id` = #{row.userId,jdbcType=BIGINT}");
        sql.SET("`book_id` = #{row.bookId,jdbcType=BIGINT}");
        sql.SET("`borrow_date` = #{row.borrowDate,jdbcType=DATE}");
        sql.SET("`return_date` = #{row.returnDate,jdbcType=DATE}");
        sql.SET("`status` = #{row.status,jdbcType=CHAR}");
        sql.SET("`created_at` = #{row.createdAt,jdbcType=TIMESTAMP}");
        sql.SET("`updated_at` = #{row.updatedAt,jdbcType=TIMESTAMP}");
        
        TransactionPoExample example = (TransactionPoExample) parameter.get("example");
        applyWhere(sql, example, true);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table Transaction
     *
     * @mbg.generated
     */
    public String updateByPrimaryKeySelective(TransactionPo row) {
        SQL sql = new SQL();
        sql.UPDATE("Transaction");
        
        if (row.getUserId() != null) {
            sql.SET("`user_id` = #{userId,jdbcType=BIGINT}");
        }
        
        if (row.getBookId() != null) {
            sql.SET("`book_id` = #{bookId,jdbcType=BIGINT}");
        }
        
        if (row.getBorrowDate() != null) {
            sql.SET("`borrow_date` = #{borrowDate,jdbcType=DATE}");
        }
        
        if (row.getReturnDate() != null) {
            sql.SET("`return_date` = #{returnDate,jdbcType=DATE}");
        }
        
        if (row.getStatus() != null) {
            sql.SET("`status` = #{status,jdbcType=CHAR}");
        }
        
        if (row.getCreatedAt() != null) {
            sql.SET("`created_at` = #{createdAt,jdbcType=TIMESTAMP}");
        }
        
        if (row.getUpdatedAt() != null) {
            sql.SET("`updated_at` = #{updatedAt,jdbcType=TIMESTAMP}");
        }
        
        sql.WHERE("`id` = #{id,jdbcType=BIGINT}");
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table Transaction
     *
     * @mbg.generated
     */
    protected void applyWhere(SQL sql, TransactionPoExample example, boolean includeExamplePhrase) {
        if (example == null) {
            return;
        }
        
        String parmPhrase1;
        String parmPhrase1_th;
        String parmPhrase2;
        String parmPhrase2_th;
        String parmPhrase3;
        String parmPhrase3_th;
        if (includeExamplePhrase) {
            parmPhrase1 = "%s #{example.oredCriteria[%d].allCriteria[%d].value}";
            parmPhrase1_th = "%s #{example.oredCriteria[%d].allCriteria[%d].value,typeHandler=%s}";
            parmPhrase2 = "%s #{example.oredCriteria[%d].allCriteria[%d].value} and #{example.oredCriteria[%d].criteria[%d].secondValue}";
            parmPhrase2_th = "%s #{example.oredCriteria[%d].allCriteria[%d].value,typeHandler=%s} and #{example.oredCriteria[%d].criteria[%d].secondValue,typeHandler=%s}";
            parmPhrase3 = "#{example.oredCriteria[%d].allCriteria[%d].value[%d]}";
            parmPhrase3_th = "#{example.oredCriteria[%d].allCriteria[%d].value[%d],typeHandler=%s}";
        } else {
            parmPhrase1 = "%s #{oredCriteria[%d].allCriteria[%d].value}";
            parmPhrase1_th = "%s #{oredCriteria[%d].allCriteria[%d].value,typeHandler=%s}";
            parmPhrase2 = "%s #{oredCriteria[%d].allCriteria[%d].value} and #{oredCriteria[%d].criteria[%d].secondValue}";
            parmPhrase2_th = "%s #{oredCriteria[%d].allCriteria[%d].value,typeHandler=%s} and #{oredCriteria[%d].criteria[%d].secondValue,typeHandler=%s}";
            parmPhrase3 = "#{oredCriteria[%d].allCriteria[%d].value[%d]}";
            parmPhrase3_th = "#{oredCriteria[%d].allCriteria[%d].value[%d],typeHandler=%s}";
        }
        
        StringBuilder sb = new StringBuilder();
        List<Criteria> oredCriteria = example.getOredCriteria();
        boolean firstCriteria = true;
        for (int i = 0; i < oredCriteria.size(); i++) {
            Criteria criteria = oredCriteria.get(i);
            if (criteria.isValid()) {
                if (firstCriteria) {
                    firstCriteria = false;
                } else {
                    sb.append(" or ");
                }
                
                sb.append('(');
                List<Criterion> criterions = criteria.getAllCriteria();
                boolean firstCriterion = true;
                for (int j = 0; j < criterions.size(); j++) {
                    Criterion criterion = criterions.get(j);
                    if (firstCriterion) {
                        firstCriterion = false;
                    } else {
                        sb.append(" and ");
                    }
                    
                    if (criterion.isNoValue()) {
                        sb.append(criterion.getCondition());
                    } else if (criterion.isSingleValue()) {
                        if (criterion.getTypeHandler() == null) {
                            sb.append(String.format(parmPhrase1, criterion.getCondition(), i, j));
                        } else {
                            sb.append(String.format(parmPhrase1_th, criterion.getCondition(), i, j,criterion.getTypeHandler()));
                        }
                    } else if (criterion.isBetweenValue()) {
                        if (criterion.getTypeHandler() == null) {
                            sb.append(String.format(parmPhrase2, criterion.getCondition(), i, j, i, j));
                        } else {
                            sb.append(String.format(parmPhrase2_th, criterion.getCondition(), i, j, criterion.getTypeHandler(), i, j, criterion.getTypeHandler()));
                        }
                    } else if (criterion.isListValue()) {
                        sb.append(criterion.getCondition());
                        sb.append(" (");
                        List<?> listItems = (List<?>) criterion.getValue();
                        boolean comma = false;
                        for (int k = 0; k < listItems.size(); k++) {
                            if (comma) {
                                sb.append(", ");
                            } else {
                                comma = true;
                            }
                            if (criterion.getTypeHandler() == null) {
                                sb.append(String.format(parmPhrase3, i, j, k));
                            } else {
                                sb.append(String.format(parmPhrase3_th, i, j, k, criterion.getTypeHandler()));
                            }
                        }
                        sb.append(')');
                    }
                }
                sb.append(')');
            }
        }
        
        if (sb.length() > 0) {
            sql.WHERE(sb.toString());
        }
    }
}